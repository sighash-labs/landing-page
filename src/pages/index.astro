---
import BaseLayout from '../layouts/BaseLayout.astro';

const services = [
  {
    title: 'Smart Contract Audits',
    copy: 'In-depth reviews of your smart contracts to identify vulnerabilities before they reach production.',
  },
  {
    title: 'Protocol Security Reviews',
    copy: 'End-to-end analysis of protocol design, economic assumptions, and upgrade paths.',
  },
  {
    title: 'Security Consulting & Threat Modeling',
    copy: 'Hands-on guidance to map attack surfaces, prioritize fixes, and strengthen your security processes.',
  },
];

const processSteps = [
  { title: 'Scope', detail: 'We align on architecture, assumptions, timelines, and depth of review.' },
  { title: 'Review & Analysis', detail: 'Manual code reads, dynamic testing, and scenario design tailored to your stack.' },
  { title: 'Reporting', detail: 'Clear severity ratings, repro steps, and fixes prioritized for engineering teams.' },
  { title: 'Remediation Support', detail: 'Rapid follow-ups to verify patches and keep launches on schedule.' },
];

const valueProps = [
  'Direct access to the auditors performing the work.',
  'Tool-assisted coverage without sacrificing manual depth.',
  'Transparent timelines, fixed scopes, and actionable guidance.',
];
---

<BaseLayout
  title="Sighash Labs | Blockchain Security Audits"
  description="Blockchain security audits for teams shipping protocols that cannot fail. Smart contract reviews, protocol hardening, and remediation support."
>
  <section class="relative isolate overflow-hidden">
    <div class="absolute -left-16 top-16 h-72 w-72 rounded-full bg-emerald-500/20 blur-[120px]" aria-hidden="true"></div>
    <div class="absolute -right-20 bottom-0 h-80 w-80 rounded-full bg-sky-500/10 blur-[140px]" aria-hidden="true"></div>
    <div class="mx-auto flex max-w-6xl flex-col items-center px-6 pt-28 pb-20 text-center">
      <h1 class="mt-6 text-4xl font-semibold tracking-tight text-white sm:text-5xl lg:text-6xl">
        Blockchain security for
        <span class="text-rotator ml-3" aria-live="polite">
          <span class="text-rotator-line">critical launches</span>
          <span class="text-rotator-line">protocol rollouts</span>
          <span class="text-rotator-line">founder roadmaps</span>
          <span class="text-rotator-line">ecosystem upgrades</span>
          <span class="text-rotator-line">high-stakes teams</span>
        </span>
      </h1>
      <p class="mt-6 max-w-3xl text-lg text-slate-300">
        Blockchain security audits for protocols that can’t afford to fail. We partner with founders to harden smart contracts and ship with confidence on any chain.
      </p>
      <div class="mt-10 flex flex-col gap-4 sm:flex-row">
        <a
          href="mailto:info@sighash.xyz"
          class="inline-flex items-center justify-center rounded-full bg-emerald-500 px-8 py-3 text-base font-semibold text-white shadow-xl shadow-emerald-500/40 transition hover:-translate-y-0.5 hover:bg-emerald-400 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-emerald-300"
        >
          Request an audit
        </a>
        <a
          href="/audits"
          class="inline-flex items-center justify-center rounded-full border border-white/10 px-8 py-3 text-base font-semibold text-white/80 transition hover:border-white/40 hover:text-white"
        >
          View audits
        </a>
      </div>
    </div>
  </section>

  <section class="mx-auto max-w-6xl px-6 py-16">
    <div class="flex flex-col items-start gap-6 text-left md:flex-row md:items-center md:justify-between">
      <div>
        <p class="eyebrow">What we do</p>
        <h2 class="mt-3 text-3xl font-semibold text-white">Security reviews for protocol teams</h2>
        <p class="mt-4 max-w-2xl text-base text-slate-300">
          We work with teams building high-impact blockchain protocols to reduce risk before launch and ahead of major upgrades.
          Every engagement is scoped to your architecture, timelines, and risk tolerance.
        </p>
      </div>
      <a href="mailto:info@sighash.xyz" class="text-sm font-semibold text-emerald-300 transition hover:text-white">
        Get scoped →
      </a>
    </div>
    <div class="mt-10 grid gap-6 md:grid-cols-3">
      {services.map((service) => (
        <div class="glass-panel flex h-full flex-col gap-3 p-6">
          <h3 class="text-xl font-semibold text-white">{service.title}</h3>
          <p class="text-sm text-slate-300">{service.copy}</p>
        </div>
      ))}
    </div>
  </section>

  <section class="bg-slate-900/40">
    <div class="mx-auto max-w-6xl px-6 py-16">
      <div class="max-w-3xl">
        <p class="eyebrow">How we work</p>
        <h2 class="mt-3 text-3xl font-semibold text-white">A predictable audit process</h2>
        <p class="mt-4 text-base text-slate-300">
          Fast turnarounds without sacrificing review depth. We stay close to your engineering team from kickoff through fixes.
        </p>
      </div>
      <div class="mt-10 grid gap-6 md:grid-cols-2">
        {processSteps.map((step, index) => (
          <div class="rounded-2xl border border-white/10 bg-slate-950/40 p-6 shadow-lg shadow-black/20">
            <span class="text-sm font-semibold text-emerald-300">0{index + 1}</span>
            <h3 class="mt-3 text-xl font-semibold text-white">{step.title}</h3>
            <p class="mt-3 text-sm text-slate-300">{step.detail}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="mx-auto max-w-6xl px-6 py-16">
    <div class="grid gap-10 lg:grid-cols-[2fr_1fr]">
      <div>
        <p class="eyebrow">Why sighash</p>
        <h2 class="mt-3 text-3xl font-semibold text-white">Security partners, not vendors</h2>
        <p class="mt-4 text-base text-slate-300">
          We’ve spent the last several cycles hardening blockchain infrastructure. Our perspective helps teams avoid the traps we’ve
          seen across ecosystems.
        </p>
        <ul class="mt-8 space-y-4">
          {valueProps.map((point) => (
            <li class="flex items-start gap-3 text-sm text-slate-200">
              <span class="mt-1 inline-flex h-2.5 w-2.5 rounded-full bg-emerald-400" aria-hidden="true"></span>
              <span>{point}</span>
            </li>
          ))}
        </ul>
      </div>
      <div class="glass-panel flex flex-col justify-between p-6">
        <div>
          <p class="text-sm font-semibold text-emerald-200">Featured work</p>
          <h3 class="mt-3 text-2xl font-semibold text-white">Audits library</h3>
          <p class="mt-3 text-sm text-slate-300">
            Explore published engagements, scope summaries, and release-ready recommendations from the Sighash Labs team.
          </p>
        </div>
        <a
          href="/audits"
          class="mt-8 inline-flex items-center justify-center rounded-full bg-white/10 px-6 py-3 text-sm font-semibold text-white transition hover:bg-white/20"
        >
          Explore past audits →
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
